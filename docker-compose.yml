services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    env_file:
      - ./api/.env

  dashboard:
    build: ./dashboard
    ports:
      - "8501:8501"
    depends_on:
      - api
    environment:
      - API_BASE_URL=http://api:8000
